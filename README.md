# Hackathon-2026




# HELPFUL GIT COMMANDS
git pull
git checkout -b your-branch-name
#### -b creates new branch, just use git checkout branch to switch

# make changes
git add .
git commit -m "Describe changes"
git push origin your-branch-name